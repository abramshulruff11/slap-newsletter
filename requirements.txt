# PINNED (SLA-57, 2026-09-23). Every entry below is pinned to the version
# GitHub Actions run 35602375183 (2026-09-21, green end to end) actually
# installed. An unpinned entry can update overnight and break a run with no
# code change on our end — that's exactly how the 2026-09-01 anthropic
# outage happened. To bump any entry deliberately: change one version, run
# the pipeline via workflow_dispatch, confirm green, then leave it pinned
# forward. Don't pin from a local `pip freeze` — local and CI have genuinely
# different installed versions (see SLA-57).
feedparser==6.0.14
# The 2026-09-01 Pass 1 outage was a client-side SDK guard that arrived with
# no commit of ours: a bare `anthropic` resolves whatever is newest on every
# CI run, so the pipeline could break overnight with nothing to diff. 1.2.0
# is the version run 169 proved green end to end (1.3.0 had already shipped
# by 2026-09-02, untested here). Bump deliberately, and re-run the pipeline
# on the new version before leaving it pinned forward.
anthropic==1.2.0
python-dotenv==1.2.3
beautifulsoup4==4.15.0
requests==2.34.2
tzdata==2026.4
# Pins the Python package only, not the Chromium build `playwright install`
# downloads at run time — see the Chromium note in CLAUDE.md's Known Issues.
playwright==1.63.0
Pillow==12.3.0
python-substack==0.1.22
curl_cffi==0.16.3
# SLA-65: reads each league's defending champion from slap-sports-db. The
# same version that repo's CI runs green on Python 3.12, where the pipeline
# runs. Imported only when SPORTS_DB_URL is set.
psycopg[binary]==3.2.3
